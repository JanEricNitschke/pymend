# Patch generated by Pymend v3.2.2

--- a//tmp/pymend-pr335/tests/test_pymend/refs/skip_overload_decorator.py
+++ b//tmp/pymend-pr335/tests/test_pymend/refs/skip_overload_decorator.py
@@ -11,5 +11,15 @@
     """Turn an expression ast node into a sensible string.
 
     Used for default values and annotations.
+
+    Parameters
+    ----------
+    annotation : Optional[ast.expr]
+        _description_
+
+    Returns
+    -------
+    Optional[str]
+        _description_
     """
     return None if annotation is None else ast.unparse(annotation)
